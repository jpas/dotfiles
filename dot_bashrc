# do not do anything if we are not interactive
[[ $- == *i* ]] || return

if [[ -d $HOME/.config/bash/rc.d ]]; then
  for f in $HOME/.config/bash/rc.d/*.bash; do
    if [[ -r $f ]]; then
      source $f
    fi
  done
fi
